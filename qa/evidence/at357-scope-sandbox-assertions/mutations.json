{
  "tests": [
    "tests/test_mutation_sandbox.py"
  ],
  "mutations": [
    {
      "name": "the sandbox is never removed - AT-325's leak returns",
      "file": "scripts/mutation_check.py",
      "old": "    shutil.rmtree(root, ignore_errors=True)",
      "new": "    return",
      "kills": [
        "tests/test_mutation_sandbox.py::test_the_sandbox_is_removed_when_the_run_finishes",
        "tests/test_mutation_sandbox.py::test_the_sandbox_is_removed_even_when_the_run_is_refused"
      ]
    },
    {
      "name": "AT-357: cleanup sweeps the temp dir by glob and eats a concurrent run's live sandbox",
      "file": "scripts/mutation_check.py",
      "old": "    shutil.rmtree(root, ignore_errors=True)",
      "new": "    for path in temp.glob(\"mutation-check-*\"):\n        shutil.rmtree(path, ignore_errors=True)",
      "kills": [
        "tests/test_mutation_sandbox.py::test_a_concurrent_runs_sandbox_is_left_alone"
      ]
    },
    {
      "name": "AT-357: the temp-root redirection is dropped, so every leak assertion goes vacuous",
      "file": "tests/test_mutation_sandbox.py",
      "old": "    monkeypatch.setattr(tempfile, \"tempdir\", str(root))",
      "new": "    pass",
      "kills": [
        "tests/test_mutation_sandbox.py::test_the_sandbox_really_is_created_under_the_private_root"
      ]
    }
  ]
}
