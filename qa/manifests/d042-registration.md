# Manifest — d042-registration
**Contract:** docs/DECISIONS.md D-042 (Changes-authorized: `.goal/goal.json`, `tests/test_goal_done_checks.py`, `target.md` in this unit)
**Goal task:** none (governance/registration unit — registers T-179, T-180, T-181 themselves)
**Date:** 2026-09-24
**Fix cycle:** 1 of max 3
**Dual check:** no
**Issues addressed:** none
**Executor:** claude-sonnet-subagent
**Executor rationale:** governance data edit (goal.json rows, one pinning test, one roadmap doc) — no external delegation warranted, and D-042 requires the exact task briefed by the orchestrator
**Delegation:** n/a (Claude subagent)

## What changed
- `.goal/goal.json` `tasks[]` — appended three rows at the end, same shape as T-172..T-178:
  - `T-179` "Deep Agents lead tester + stage tools (crawl, run_case, get_persona, get_catalog, capture_network, grade_case, report) with deterministic guards inside each tool (D-042)" — `deps: [T-170, T-172, T-175]`, `base_criticality`/`criticality: "high"`, `user_value: "high"`, `done_check.cmd: "uv run pytest tests/test_agent_layer.py"`.
  - `T-180` "Subagents: explorer, test-designer, runner, independent grader (no action tools, C7), reporter (D-042)" — `deps: [T-179]`, `"high"`, `done_check.cmd: "uv run pytest tests/test_agent_subagents.py"`.
  - `T-181` "Agent guardrails + measured gain vs pipeline (bugs found, FP, tokens, time) on a fixture; per-run token/cost budget (D-042)" — `deps: [T-180]`, `"high"`, `done_check.cmd: "uv run pytest tests/test_agent_gain.py"`.
  - All three `base_criticality`/`criticality` values are `"high"`, in the classifier vocabulary `{"low","medium","high","critical"}` (`tests/test_goal_criticality_vocabulary.py`) — no out-of-vocabulary risk.
  - `T-167` (`.goal/goal.json` task row) `note` — appended ` | D-042: runs the lead agent on LangGraph checkpoints.` to the existing D-041 note (nothing else on the row touched).
  - `T-177` (`.goal/goal.json` task row) `note` — appended ` | D-042: becomes a runner-subagent tool.` to the existing D-041 note.
  - Top-level `progress`, `analytics`, `updated`, `last_deterministic_tick`, and `notifications[]` — refreshed by the instructed `python D:/ai_os/.claude/skills/goal/scripts/monitor.py "<worktree>"` run (side effect, not hand-edited): `total` 64→67, `pending` 26→29, `percent` 59→57, plus one `needs review` notification per new task (standard for any newly-registered high-criticality row).
  - No other task row was touched.
- `.goal/dashboard.html` — regenerated by the same `monitor.py` run (progress numbers only, not hand-edited).
- `tests/test_goal_done_checks.py::test_revised_goal_contract_is_registered` — pinned the three new rows into the `expected` dict (`"T-179": (["T-170", "T-172", "T-175"], "uv run pytest tests/test_agent_layer.py")`, `"T-180": (["T-179"], "uv run pytest tests/test_agent_subagents.py")`, `"T-181": (["T-180"], "uv run pytest tests/test_agent_gain.py")`), and changed the task-count assertion `== 64` → `== 67` with an updated inline comment (`# D-040: T-170, T-171; D-041: T-172..T-178; D-042: T-179..T-181`), split onto its own comment line to satisfy ruff `E501` (line was 121 chars inline).
- `target.md` "M10b · Platform & AI-framework layer" — heading now notes the Agents-row supersession; the Agents row now reads "LangChain Deep Agents lead tester + subagents (explorer, designer, runner, independent grader, reporter), after Wave 1 — T-179..T-181 (D-042, supersedes D-041's no-multi-agent)"; added one new bullet stating prompts ship as `SKILL.md` skills (T-175) while the seven named stages stay plain tools with deterministic guards inside them; Progress line updated to the regenerated `38 / 67 done (57%)` with T-179..T-181 added to the "Next" list.
- `docs/SNAPSHOT.md` — regenerated by the instructed `autotester snapshot` verify step (side effect, not hand-edited): last-decisions block now shows D-041 as `SUPERSEDED (by D-042)` and lists D-042 `ACTIVE`.

## How to verify (commands + expected)
- `PYTHONUTF8=1 uv run --project . --directory . pytest tests/test_goal_done_checks.py tests/test_goal_criticality_vocabulary.py` → expect exit 0, all passed
- `PYTHONUTF8=1 uv run --project . --directory . ruff check src tests scripts` → expect "All checks passed!"
- `PYTHONUTF8=1 uv run --project . --directory . autotester snapshot` → expect a line count report, exit 0
- `PYTHONUTF8=1 uv run --project . --directory . autotester doctor` → expect "doctor: clean"

## Actual outputs (from maker's own run)
```
$ PYTHONUTF8=1 uv run --project . --directory . pytest tests/test_goal_done_checks.py tests/test_goal_criticality_vocabulary.py
..........                                                               [100%]
10 passed in 0.21s

$ PYTHONUTF8=1 uv run --project . --directory . ruff check src tests scripts
All checks passed!

$ PYTHONUTF8=1 uv run --project . --directory . autotester snapshot
snapshot: 44 lines written

$ PYTHONUTF8=1 uv run --project . --directory . autotester doctor
doctor: clean
```

**Declared gap (per the brief):** the project's FULL test suite (`uv run pytest`, no target filter) was deliberately NOT run in this unit — RAM is tight and another checker session was running a full suite concurrently (per the dispatch brief). Only the two directly-relevant test modules above were run, plus ruff (whole `src tests scripts` tree) and `autotester doctor`/`snapshot` (whole-project static checks). The change is governance data (three `goal.json` rows, one pinning test, one roadmap doc) with no runtime/import-graph impact, so the blast radius the full suite would additionally cover is judged low — but this is a stated gap, not a claim of full coverage.

## Capability coverage (each new claim -> its isolating falsification)

| capability (one line) | the check that covers it | the falsifying edit | observed (pasted runner output) |
|---|---|---|---|
| The registered T-179/T-180/T-181 rows (deps + done_check cmd) and the task count (67) are pinned so a later drift on any of them is caught | `tests/test_goal_done_checks.py::test_revised_goal_contract_is_registered` | Change T-180's `deps` in `.goal/goal.json` from `["T-179"]` to `["T-999"]` (single hunk), reproduced in a throwaway copy at `C:\Users\Lenovo\AppData\Local\Temp\claude\d--autoTesting\dd410a44-7522-428c-9b91-fda96de822cd\scratchpad\goal-falsify.json`, never in the worktree | **Before/after, via a script that loads the test module and points its `GOAL` constant at each file in turn (never the live worktree file for the "after" mutation — that file was a scratchpad copy):** against the **mutated throwaway copy** (`T-180 deps: ['T-999']`) → `test_revised_goal_contract_is_registered` raises `AssertionError` (actual `deps` dict entry for `T-180` no longer equals the pinned `expected` dict) — reddens for exactly that row, nothing else touched. Against the **unmutated worktree file** (`T-180 deps: ['T-179']`) → same test **PASSED**. Confirmed again with the real pytest runner on the unmutated worktree file: `pytest tests/test_goal_done_checks.py::test_revised_goal_contract_is_registered -v` → `1 passed in 0.17s`. |

No other new capability claims in this unit — it is governance data (goal-tracker rows + a roadmap doc), not executable behaviour; the pinning test above is the only thing there is to falsify.

## Live browser evidence
Not UI-touching — no surface changed. Only `.goal/goal.json` (data), `.goal/dashboard.html` (regenerated static report page, via the standard `monitor.py` regen step), `tests/test_goal_done_checks.py` (a pinning test), `target.md` (a markdown roadmap doc), and `docs/SNAPSHOT.md` (regenerated static snapshot) changed; no `*.tsx|jsx|vue|svelte|html|css` application UI, route, page, or component file was touched.

## Status: checked-PASS (qa/verdicts/d042-registration.md, cycle 1, 2026-09-24)
