# Manifest — fix-t175-criticality
**Contract:** qa/contracts/core-invariants.md (no dedicated goal/criticality contract exists; this unit corrects a data value against `tests/test_goal_criticality_vocabulary.py`, which pins the classifier vocabulary contract in code per AT-116)
**Goal task:** T-175
**Date:** 2026-09-24
**Fix cycle:** 1 of max 3
**Dual check:** no
**Issues addressed:** none — found by the T-170 builder's full-suite run
**Executor:** claude-sonnet-subagent
**Executor rationale:** single-value data fix in `.goal/goal.json`, no external delegation warranted
**Delegation:** n/a (Claude subagent)

## What changed
- `.goal/goal.json:1213-1214` (T-175 task row) — `base_criticality` changed from `"normal"` to `"medium"`, `criticality` changed from `"low"` to `"medium"`. Commit ea6b7c3 registered T-175 with `base_criticality: "normal"`, which is not in the classifier vocabulary `{"low","medium","high","critical"}` (`tests/test_goal_criticality_vocabulary.py:31`); the classifier silently downgrades unrecognised values to `"low"` (hence the stale `criticality: "low"` on the same row). T-175 is a behaviour-preserving refactor (prompts-as-SKILL.md) with `user_value: "normal"` (a separate field, left untouched), so `"medium"` is the correct mapped value — not `"high"` or `"critical"`.
- `.goal/goal.json` top-level `updated`, `analytics.velocity_per_day`, `analytics.eta_days`, `last_deterministic_tick` — refreshed as a side effect of running `D:/ai_os/.claude/skills/goal/scripts/monitor.py` to regenerate the dashboard (instructed step). No other task rows were touched — verified by scripted scan below.
- `.goal/dashboard.html` — regenerated by the same monitor.py run (1-line diff, timestamp-driven).
- No other file changed. No other task's `base_criticality` is out of vocabulary (scanned all 64 rows; T-175 was the only offender).

## How to verify (commands + expected)
- `PYTHONUTF8=1 uv run --project . --directory . pytest tests/test_goal_criticality_vocabulary.py tests/test_goal_done_checks.py` → expect exit 0, all passed
- `PYTHONUTF8=1 uv run --project . --directory . ruff check src tests scripts` → expect "All checks passed!"
- `PYTHONUTF8=1 uv run --project . --directory . autotester doctor` → expect "doctor: clean"

## Actual outputs (from maker's own run)
```
$ PYTHONUTF8=1 uv run --project . --directory . pytest tests/test_goal_criticality_vocabulary.py tests/test_goal_done_checks.py
..........                                                               [100%]
10 passed in 0.14s

$ PYTHONUTF8=1 uv run --project . --directory . ruff check src tests scripts
All checks passed!

$ PYTHONUTF8=1 uv run --project . --directory . autotester doctor
doctor: clean
```

**Declared gap (for the checker):** the project's FULL test suite (`uv run pytest`, no target filter) was deliberately NOT run in this unit — RAM is tight and a checker/T-170 process was running a full suite concurrently. Only the two directly-relevant modules above were run, plus ruff and doctor across the whole tree. The change is a single-value edit confined to `.goal/goal.json` (a data file, not imported code), so the blast radius for anything the two targeted test files + ruff + doctor would miss is judged low, but this is a stated gap, not a claim of full coverage.

## Capability coverage (each new claim -> its isolating falsification)

| capability (one line) | the check that covers it | the falsifying edit | observed (pasted runner output) |
|---|---|---|---|
| T-175's `base_criticality`/`criticality` are values the classifier vocabulary recognises | `tests/test_goal_criticality_vocabulary.py::test_every_base_criticality_is_a_value_the_classifier_recognises` | set T-175's `base_criticality` back to `"normal"` / `criticality` back to `"low"` (single hunk, done in a throwaway copy of goal.json outside the worktree, at `C:\Users\Lenovo\AppData\Local\Temp\claude\d--autoTesting\...\scratchpad\goal-falsify.json`, never in the worktree) | **Before fix (measured on master/pre-fix worktree state):** `pytest tests/test_goal_criticality_vocabulary.py` → `FAILED tests/test_goal_criticality_vocabulary.py::test_every_base_criticality_is_a_value_the_classifier_recognises` — `AssertionError: these tasks declare a criticality the classifier will silently ignore, downgrading them to 'low': {'T-175': 'normal'}` (`1 failed, 2 passed`). **Falsifying-edit re-check (throwaway copy, scripted against the same vocabulary set the test uses):** `unknown = {'T-175': 'normal'}` — same exact failure reproduced, isolating precisely the T-175 row. **After fix (worktree, current state):** `pytest tests/test_goal_criticality_vocabulary.py tests/test_goal_done_checks.py` → `10 passed in 0.14s`. |

No other new capability claims — this unit is a single data-value correction with no new behaviour.

## Live browser evidence
Not UI-touching — no surface changed. Only `.goal/goal.json` (data) and `.goal/dashboard.html` (regenerated static report page, via the standard `monitor.py` regen step, not hand-edited) changed; no `*.tsx|jsx|vue|svelte|html|css` application UI, route, page, or component file was touched.

## Status: checked-PASS (qa/verdicts/fix-t175-criticality.md, cycle 1, 2026-09-24)
