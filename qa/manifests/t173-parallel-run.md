# Manifest — t173-parallel-run

**Contract:** qa/contracts/parallel-run.md (PR1-PR7) + qa/contracts/core-invariants.md + qa/contracts/consent.md
**Goal task:** T-173
**Date:** 2026-09-25
**Fix cycle:** 1 of 3
**Dual check:** no  ← `.goal/goal.json` T-173 `criticality: "high"`, not `critical`
**Issues addressed:** none
**Executor:** claude-sonnet-subagent
**Executor rationale:** schema addition (`Project.max_parallel`, `Run.parallel_*`) plus a new
concurrency/consent module touching `RunApproval` bounds — stays on a Claude subagent per the
maker skill's "never delegate to an Ollama lane: schema/security/auth units" rule (this unit is
both schema and consent-adjacent).

## What changed

- `src/autotester/schema/project.py:133-137` — added `Project.max_parallel: int` (default 1,
  `ge=1`), the config ceiling half of PR1's `min()`.
- `src/autotester/schema/run.py:6,65-74` — added `Run.parallel_n: int | None` and
  `Run.parallel_bound_by: Literal["config","budget","write_policy"] | None`, PR1's "the run's
  persisted state records both the chosen N and which term bound it".
- `src/autotester/stages/parallel_run.py` (new, 237 lines) — the fan-out module: RAM
  measurement (`_free_ram_mb`, Windows `GlobalMemoryStatusEx` / POSIX `/proc/meminfo`),
  `plan_parallel_run` (PR1 + PR3), `RunBudget` (PR7, thread-safe aggregate consent bound),
  `run_cases` (PR2/PR4/PR5/PR6 — one function, concurrency width `plan.n`, so `n=1` is exactly
  the serial baseline PR4/PR5 diff against), `default_session_factory` (PR2's real integration —
  one `BrowserSession` per case, on its own `case.id`-scoped `ProjectPaths` profile dir).
  New module, not an addition to `stages/execute.py`: fan-out orchestration is a different
  concern from running one case's own steps (contract's own "How a unit is verified" anticipates
  this: "the fan-out logic lands in a new, justified module").
- `tests/test_parallel_run.py` (new, 295 lines) — 10 tests, one section per PR criterion, fakes
  only (no real Playwright browser; `plan_parallel_run`'s RAM/CPU inputs always injected).
- `docs/MAP.md` — regenerated by `autotester map` (module docstring for the new file).

**Not touched:** `stages/execute.py`, `ui/routes_runs.py`, `stages/orchestrate.py`,
`providers/base.py` — no wiring into the live UI run path in this unit (T-172 and T-175 are
building in sibling worktrees against `orchestrate.py`/`providers/base.py`, named as avoid-files
in this unit's brief; the capability itself — `run_cases` + `plan_parallel_run` — is complete and
independently tested, ready for a follow-up unit to call from `routes_runs.py`/`orchestrate.py`
without touching either file's internals here).

## How to verify (commands + expected)

- `uv run pytest tests/test_parallel_run.py` → expected: exit 0, 10 passed (this is T-173's
  `done_check` in `.goal/goal.json`)
- `uv run ruff check src tests scripts` → expected: exit 0
- `uv run autotester doctor` → expected: exit 0

## Actual outputs (from maker's own run)

```
$ uv run pytest tests/test_parallel_run.py
..........                                                               [100%]
10 passed in 1.44s

$ uv run ruff check src tests scripts
All checks passed!

$ uv run autotester doctor
doctor: clean
```

**Full suite (`uv run pytest`, no CLI `-q`) — NOT RUN, RAM ceiling.** Measured across four
checks over ~5 minutes while building this unit (`Get-CimInstance Win32_OperatingSystem
FreePhysicalMemory` + `Get-CimInstance Win32_Process` filtered to `*pytest*autoTesting*`):

| t+ | free RAM | other D:/autoTesting pytest procs |
|---|---|---|
| 0 min | 0.71 GB | 16 |
| ~2 min | 2.45 GB | — |
| ~4 min | 4.21 GB | 10 |
| ~6 min | 2.95 GB | 6 |

The ≥3.5 GB-free-AND-zero-sibling-pytest window never held simultaneously — RAM crossed 3.5 GB
once (t+4min) but 10 sibling `pytest` processes (T-172/T-175/other waves) were still running at
that instant, and by the next check RAM had fallen back under the threshold while siblings were
still active. Per the brief's RAM rule this is declared **FULL SUITE NOT RUN — RAM ceiling;
checker to run it**, rather than polled for the full 60 minutes against a host that is visibly
mid-wave on several other units (running it anyway would also compete with those sessions for
the same scarce RAM). Targeted `tests/test_parallel_run.py` + `ruff` + `doctor` above are green
and are what T-173's own `done_check` requires.

## Capability coverage (each new claim -> its isolating falsification)

All falsifying edits were made and run in a throwaway copy OUTSIDE the worktree
(`%TEMP%\claude\d--autoTesting\<session>\scratchpad\falsify-repo`, a standalone `uv`-built copy
of `src/`, `tests/`, `scripts/`, `pyproject.toml`), never against the working tree. Baseline
(unmodified copy) was green (10 passed) before every sabotage; each sabotage's anchor was
asserted to match exactly once and the file to actually change (core-invariants C7) before its
result was believed; each was reverted from a saved pristine backup and re-confirmed green
before the next.

| capability (PR) | the check that covers it | the falsifying edit | observed (pasted runner output) |
|---|---|---|---|
| PR1 — N = min(config ceiling, measured budget), binding recorded | `test_pr1_n_is_min_of_config_ceiling_and_measured_budget` (`tests/test_parallel_run.py:74`) | `parallel_run.py:126` `n = min(config_ceiling, measured_budget)` → `n = config_ceiling` (ignore the measured budget) | BEFORE: `1 passed` (this test alone). AFTER: `AssertionError: assert (8, 'budget', 8, 3) == (3, 'budget', 8, 3)` — `1 failed, 1 passed`. REVERTED: `2 passed, 8 deselected`. |
| PR2 — each case gets an isolated session (own profile dir), no shared session leak | `test_pr2_each_case_gets_its_own_session_no_shared_state` + `test_pr2_default_session_factory_gives_each_case_a_distinct_profile` (`tests/test_parallel_run.py:106,126`) | `parallel_run.py:232` `ProjectPaths(f"{project.slug}-parallel-{case.id[:12]}")` → `ProjectPaths(f"{project.slug}-parallel")` (same slug for every case) | BEFORE: `2 passed`. AFTER: `AssertionError: assert 'p1-parallel' != 'p1-parallel'` — `1 failed, 1 passed`. REVERTED: `2 passed`. |
| PR3 — `write_policy=ALLOW_WRITES` forces serial regardless of `max_parallel` | `test_pr3_allow_writes_forces_serial_regardless_of_max_parallel` (`tests/test_parallel_run.py:149`) | `parallel_run.py:122` `if project.write_policy is WritePolicy.ALLOW_WRITES:` → `if False:` (never force serial) | BEFORE: `1 passed`. AFTER: `AssertionError: assert (4, 'config') == (1, 'write_policy')` — `1 failed, 9 deselected`. REVERTED: `1 passed, 9 deselected`. |
| PR4 — verdict parity AND case order survive concurrency (parallel == serial, no reordering) | `test_pr4_parallel_verdicts_equal_serial_verdicts` (`tests/test_parallel_run.py:187`, incl. its final order assertion) | `parallel_run.py:211` `return [f.result() for f in futures]` → `return [f.result() for f in reversed(futures)]` | BEFORE: `1 passed`. AFTER: `AssertionError: assert ['case_fcaf65...'] == ['case_3531e5...']` (order mismatch; the outcome-parity dict-equality assertion on the line above stayed green, confirming the two assertions isolate independent properties) — `1 failed, 9 deselected`. REVERTED: `1 passed, 9 deselected`. |
| PR5 — parallel wall-clock strictly less than serial for N>=2 | `test_pr5_parallel_is_faster_than_serial_for_n_ge_2` (`tests/test_parallel_run.py:203`, asserts `parallel_s < serial_s * 0.7` — a margin against scheduler jitter, still exactly PR5's "strictly less") | `parallel_run.py:208` `n = max(1, plan.n)` → `n = 1` (always serial, ignore `plan.n`) | BEFORE: `1 passed`. AFTER: `assert 0.609... < (0.610... * 0.7)` — `1 failed, 9 deselected`. REVERTED: `1 passed, 9 deselected`. (First attempt at this sabotage used the un-margined `parallel_s < serial_s` assertion and passed by timing luck — recorded and fixed: the assertion now carries the 0.7 margin so the result is deterministic, not a coin flip on scheduler noise.) |
| PR6 — a crash in one case is reported as that case's own ERRORED outcome, never aborts siblings | `test_pr6_one_case_crashing_does_not_abort_siblings` (`tests/test_parallel_run.py:228`) | `parallel_run.py:189` `except Exception as exc:` → `except KeyError as exc:` (no longer catches the case's real exception) | BEFORE: `1 passed`. AFTER: unhandled `ValueError: boom mid-case` propagates out of `run_cases` itself (traceback through `Future.result()` → `_run_one` → `run_fn`) — the whole call errors, not just that case's outcome. REVERTED: `1 passed, 9 deselected`. |
| PR7 — aggregate `RunApproval` bounds apply to the whole run, never multiplied by concurrency | `test_pr7_aggregate_budget_is_not_multiplied_by_concurrency` (`tests/test_parallel_run.py:248`) | `parallel_run.py:183` `if budget is not None and not budget.try_consume(...)` → `if False:` (never consult the shared budget) | BEFORE: `2 passed`. AFTER: `AssertionError: at least one case refused by the shared budget` — all 4 cases (cost 3 each = 12) ran against `max_actions=10` unchecked — `1 failed, 1 passed`. REVERTED: `2 passed`. |

- **`observed` is pasted verbatim above** — the named test's own PASS line before each edit and
  its FAIL line (with the actual assertion that fired) after, same test node id in both.
- Each failure fired **for the reason the check is named for** (a value/behaviour assertion on
  the exact property, never a collection/import-level break reddening the whole file) — e.g.
  PR6's is an unhandled exception surfacing exactly where the try/except used to sit, PR4's is
  an order mismatch on the one line that asserts order while the parity-dict assertion one line
  above it stayed green.
- No `UNVERIFIED`/`INCONCLUSIVE` rows: every PR criterion has a real, reproduced falsification.

## Live browser evidence

**Not UI-touching — no surface changed.** Changed paths this unit: `src/autotester/schema/
project.py`, `src/autotester/schema/run.py`, `src/autotester/stages/parallel_run.py` (new),
`tests/test_parallel_run.py` (new), `docs/MAP.md` (generated). None match the UI-surface glob
(`*.tsx|jsx|vue|svelte|html|css`, `apps/web/**`, `**/routes/**`, `**/pages/**`,
`**/components/**`) and none flow into what a page renders — `ui/routes_runs.py` (the live run
trigger) is deliberately untouched in this unit (see "What changed" — Not touched), so nothing a
browser could observe changed.

## Status: checked-PASS (qa/verdicts/t173-parallel-run.md, Cycle checked: 1, supervising acceptance a5873ba; live wiring = AT-562)
