# Mode B sweep — SHARD 2 (checks 4, 4b, 5, 6) — 2026-09-22

Bound root: D:/autoTesting. Read-only shard; consolidation agent owns issues.jsonl/QUEUE.md/.last-sweep.

## Findings

1. **proposed-id-hint:** (reconfirm AT-365, no new id) · **severity:** high (unchanged) · **check:** 4b data-boundary
   **title:** `qa/adapter.json` still declares no `data_class` — MC-003 fires VIOLATION today, same as when AT-365 was filed.
   **evidence:** `python D:/ai_os/.claude/skills/_shared_validation/data_boundary.py D:/autoTesting` → exit 1, `[VIOLATION] qa\adapter.json has no "data_class"`. `qa/issues.jsonl` already carries this open as `AT-365` (2026-09-16, high, data-boundary) plus `AT-400`/`AT-415` documenting the gate `qa/gates/at365-data-class-declaration.md` (Umesh's decision on A/B/C/D is still unanswered — `Answered:` line at EOF is bare). No action needed beyond confirming AT-365 is still accurate; do not re-file.
   **suggested status:** open (existing), reconfirmed current.

2. **proposed-id-hint:** none (no defect) · **severity:** n/a · **check:** 6 gate-answered-off-disk
   **title:** Full pass over all 21 `qa/gates/*.md` files for "answered in chat/manifest/commit but not written to disk" — none found. Every gate carrying real content in its `Answered:`/`**Answered:**` line (at052, at106, at355, at526, live-crawl-target, next-unit-scope, post-login-forms, t136-model-credentials, t162-contract-approval) has that answer recorded on disk with date/choice/source, matching the corresponding commit or contract-fold where one exists (e.g. t162's 2026-09-21 chat answer is the direct source of commit `f949e68`'s source-adapters contract draft; at526's answer matches its own smoke-tested remedy).
   **evidence:** grepped `Answered:` across `qa/gates/*.md`; read the 3 that looked ambiguous in full (at106, at355, t162) — all three have complete, dated, sourced answers below the template line.
   **suggested status:** CLEAN, no finding.

3. **proposed-id-hint:** AT-5xx (new, cosmetic) · **severity:** low · **check:** 6 gate-answered-off-disk (near-miss)
   **title:** `qa/gates/t136-model-credentials.md:44` still carries a stray unfilled `**Answered:** _(pending)_` placeholder line directly above the real, dated answer at line 48 (`**Answered: 2026-09-09 — THE GATE WAS WRONG...`). Not a functional off-disk-answer defect (the real answer IS on disk), but a reader stopping at line 44 sees "pending" first.
   **evidence:** `qa/gates/t136-model-credentials.md:44` vs `:48`.
   **suggested status:** cosmetic cleanup, low priority — delete the stale placeholder line.

4. **proposed-id-hint:** AT-5xx (new) · **severity:** medium · **check:** 6 goal-drift / north-star (the target.md question)
   **title:** `target.md` (added 2026-09-22, commit `93fad7b`) is now routed as session-start reading in `CLAUDE.md:24` ("open at session start to stay on track") and presents a "two-layer goal" / North Star framing substantively different from `.goal/goal.json`'s actual `north_star` field — target.md's framing centers on "a product, not a test script", an "unexplored testing frontier", DISCOVER/MODEL as primary pipeline stages, and an 8-category issue-classification taxonomy, none of which appear in goal.json's north_star text (last amended 2026-09-09 per `docs/DECISIONS.md` D-023).
   **Does this literally trip check 6's trigger?** No — the trigger is "`.goal` north star **was edited** after the last contract amendment"; `git log -- .goal/goal.json` shows the north_star field itself has not been touched since D-023 (2026-09-09), while contracts have been amended far more recently (`qa/contracts/` commits through `bbd030d`, 2026-09-22 today). So the literal condition is inverted — contracts are *more* current than the north_star, not less.
   **Why it's still worth a finding:** target.md itself concedes the gap — its closing line reads "rewriting `.goal/goal.json`'s north star ... is a state change that needs an authorizing `docs/DECISIONS.md` entry first ... this roadmap doc does not require one" (`target.md`, final paragraph). No such D-entry exists yet (grepped `docs/DECISIONS.md` for "target.md"/"north star" — only D-023's original 2026-09-09 wording and unrelated hits). A session that reads only `CLAUDE.md`'s router row (not target.md's own footnote) will treat the reframed vision as operative without ever seeing the disclaimer, and nothing reconciles the two documents' wording. This is the soft version of "the goal moved, the source of truth did not" — the literal file wasn't edited, but a second, higher-visibility statement of the goal now exists un-reconciled with it.
   **evidence:** `CLAUDE.md:24`; `target.md` final paragraph; `git log --format="%h %ad %s" --date=short -- .goal/goal.json` (last north_star touch 2026-09-09, D-023); `git log -- qa/contracts/` (amendments through today); `docs/DECISIONS.md` lines 280-310 (D-023, the only north_star authorization on record).
   **suggested status:** new finding, severity medium, type `goal-drift` — recommend either (a) a DECISIONS entry that formally reconciles/supersedes goal.json's north_star with target.md's two-layer framing (T-163's DISCOVER/MODEL promotion is already tracked as pending and would be the natural vehicle), or (b) add target.md's own "pending a D-entry" caveat directly into the CLAUDE.md router row so it can't be missed by a session that only reads the table. Not a HUMAN_GATE/GRILL by itself — router table for the maker to action, not a re-Observe.

5. **proposed-id-hint:** none (no defect) · **severity:** n/a · **check:** 6 special-attention — feedback-inbox unfolded items
   **title:** Checked `qa/feedback-inbox.md` for the TestSprite-audit and BFS-exploration items named in the dispatch — both are already folded, not open work.
   **evidence:** TestSprite entry (`qa/feedback-inbox.md:757-809`) carries `**FOLDED: 2026-09-22 by /checker (Mode B sweep, AT-539 confirmation + inbox fold)**` (line 810) → filed as AT-540/541/542/543/544/545, with the ARCHITECTURE.md script-first claim deduped against the pre-existing open AT-253 rather than re-filed. BFS-exploration entry (`qa/feedback-inbox.md:274-329`) carries `**Status:** folded (2026-09-17, Mode B sweep consolidation)` (line 327) → AT-052 `verified`, gate `qa/gates/at052-bfs-video-corpus-grill.md` answered 2026-09-07.
   **suggested status:** CLEAN, no finding.

6. **proposed-id-hint:** none · **severity:** n/a · **check:** 4 loop.md agreement
   **title:** `qa/loop.md`'s Stop block lists all seven maker terminal states (ADVANCED, BACKLOG_EMPTY, HUMAN_GATE, STALLED, EXHAUSTED, BLOCKED, PAUSED) and its Human-gate section is not contradicted by `qa/adapter.json` — `isolation.done` is `audit-pass` (no deploy surface yet), so there's no CRITICAL deploy action the spec would silently license. Loop-design questions (can it spin / Goodhart the verifier / run a wrong answer to completion) all read clean: progress signal = checker PASS/goal-task-close; verify = pytest+ruff+doctor+contract-criteria, upgraded to `/outcome-grader` rubric for T-050/T-110/T-120; done_check is at least as strong as its contract criterion.
   **suggested status:** CLEAN, no finding.

7. **proposed-id-hint:** none · **severity:** n/a · **check:** 5 goal-coverage gap analysis
   **title:** `target.md`'s own M0–M12 milestone tracker already performs a fairly rigorous decomposition of the north star into covered/partial/target buckets, cross-checked against `.goal/goal.json`'s 35/55 (64%) stated progress as of 2026-09-22. No requirement surfaced in a fresh read that isn't already tracked as a pending task (T-163..T-169 cover the M8-M12 gaps explicitly). Did not independently re-derive line-by-line given shard scope/budget — flagging for the consolidator that target.md is credible check-5 evidence, not a substitute for one if a future full sweep wants to spot-verify specific milestone claims (e.g. the "45/677 = 6%" coverage figure under M4).
   **suggested status:** informational, no finding.

8. **proposed-id-hint:** none · **severity:** n/a · **check:** 6 GRILL rows consistency
   **title:** Current `qa/QUEUE.md` GRILL rows (AT-218 vacuous-guard policy, AT-281 two-mode acceptance thresholds, AT-402 visual_order.js structure review) match their gate files' state — `qa/gates/at218-vacuous-guard-class.md` explicitly reads "_(not yet — gate remains open)_". No stale GRILL row to close, no new GRILL trigger fired this shard (no fresh `.regrill-due` stamp exists — file absent).
   **suggested status:** CLEAN, no finding.

## 5-line summary

Ran checks 4/4b/5/6 read-only. 4b reconfirms the existing open AT-365 (adapter.json still has no `data_class`) — unchanged, no re-file needed. Check 6's gate-answered-off-disk sweep of all 21 gates found zero real violations (one cosmetic stray-placeholder at t136-model-credentials.md:44). The special-attention feedback-inbox items (TestSprite, BFS-exploration) are both already folded — clean. The target.md question: literal check-6 trigger does not fire (goal.json's north_star wasn't edited), but target.md's un-reconciled "two-layer goal" is now session-start-routed via CLAUDE.md without a DECISIONS entry aligning it to goal.json — filed as a new medium goal-drift finding for the consolidator. Checks 4 (loop.md) and 5 (goal-coverage) came back clean/adequately covered by existing target.md tracking.
