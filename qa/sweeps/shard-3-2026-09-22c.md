# Mode B sharded sweep — SHARD 3 (checks 7-9 + token-ledger + code-graph)

**Date:** 2026-09-22 · **Checker:** /checker Mode B sweep SHARD 3, fresh subagent (claude-sonnet) ·
**Bound root:** D:/autoTesting · **Read-only — writes nothing** (this file only; consolidation
agent owns qa/issues.jsonl, qa/QUEUE.md, qa/.last-sweep).

HEAD at time of shard: `93fad7b`. Baseline for "since last sweep": `00e36a8` (2026-09-22T16:25 entry
in `qa/.last-sweep`) through HEAD — the only unit PASSed in that window is AT-540 cycle-2
(`d7d8405`, verdict `qa/verdicts/at540-assertion-layer.md`, Cycle checked: 2).

---

## Special-attention verification (AT-541/542/543/550)

**AT-541/542/543 — confirmed STILL LEGITIMATELY OPEN.** Re-verified independently by reading (not
re-trusting shard-1/2 prose):
- `qa/manifests/` has no file for at541/at542/at543 (only `at540-assertion-layer.md` and
  `x10b-form-typing.md` are current). `qa/verdicts/` likewise names neither `df529f2` nor
  `a5e5b81`. No Mode A check has run against these three fixes — confirmed by direct `ls`, not by
  trusting the AT-547 issue text.
- Code fixes are real, read in place at HEAD:
  - AT-541: `src/autotester/stages/expand.py:33` declares `CaseClass.REGRESSION_ANCHOR` in
    `CLASS_DESCRIPTIONS`; `applicable_classes()` appends it last at line 76 (`classes.append(CaseClass.REGRESSION_ANCHOR)`).
  - AT-542: `src/autotester/schema/project.py:61-70` — `ProviderConfig.vision_ensemble()` splits/dedupes
    `self.vision` on commas, `return seen or ["gemini"]`. Wired at
    `src/autotester/ui/routes_sources.py:236-244` (`ensemble = [providers.get(name) for name in
    project.providers.vision_ensemble()]`, then `ready = [p for p in ensemble if p.available()]`) and
    `src/autotester/cli_video.py:248`.
  - AT-543: `src/autotester/stages/bench.py:52` — `oracle_human_trial(..., duration_s: float | None =
    None)`, docstring states `None` scores `duration_s=0.0` (unmeasured, honest). Sole production
    caller `scripts/bench_trial.py:169` (`bench.oracle_human_trial(f"trial_human_{ulid()}", corpus)`)
    now passes no literal — confirmed no `300.0` remains anywhere in that call path.
- **Verdict: all three stay OPEN.** Fix real, unverified — no manifest/verdict exists, so per Mode A
  discipline these cannot flip. No change from shard-1/2's finding; this is an independent re-derivation,
  not a rubber stamp.

**AT-550 — confirmed STILL REPRODUCES in code, read-only (not run).**
- `src/autotester/ui/routes_sources.py:236-244`: `ensemble = [providers.get(name) for name in
  project.providers.vision_ensemble()]` then `ready = [p for p in ensemble if p.available()]`. The
  inline comment (`:236-238`) says "the ensemble degrades, never dies" but the list comprehension
  drops any provider whose `.available()` is False with **no evidence item, no log line, no response
  field naming which provider was skipped**. `ProviderError` only fires when `ready` is empty
  (`:240-242`) — a 2-of-2 config silently running as 1-of-2 produces no signal anywhere in the
  artifact.
- `src/autotester/schema/project.py:70`: `return seen or ["gemini"]` — an empty/blank `vision` config
  string silently substitutes the literal default `["gemini"]`, no refusal, no note.
- Both lines are byte-for-byte as AT-550 (and AT-542's own docstring, `project.py:52-59`) describe.
  Still open, still correctly filed, no duplicate needed.

---

## Check 7 — Silent-failure hunt (code touched by units PASSed since last sweep)

Only product file touched by the one PASSed unit (AT-540, `d7d8405`):
`src/autotester/browser/assertions.py`. Full read + diff against parent `d7c2dfa`.

**FINDING (new) — `met()`'s url branch is still fail-UNSAFE; the AT-540 fix closed the
body_text/absent_text half of AT-551 but not the url half, and that gap is undocumented in the
ledger.**
- `assertions.py:23-44` — `met()` wraps its entire probe in a blanket
  `with contextlib.suppress(Exception): ... ; return True` (line 44 outside the `with`). The
  `expected.url and expected.url not in session.page.url` check at line 31 reads `session.page.url`
  directly, with **no per-branch guard** — unlike `body_text()` (now returns `None` on error,
  fixed) and `selector_exists()` (already suppresses internally, fixed). If `session.page.url`
  raises (closed/crashed page), the exception is swallowed by the outer `suppress` and execution
  falls through to `return True` — i.e. `met()` reports the expectation **satisfied** on a probe
  failure for any `expected.url`-bearing expectation, and the `dom_asserts` loop that would follow
  is never reached (the exception exits the `if` mid-evaluation, before the loop). This is exactly
  the "default masks a failure" pattern AT-551 was filed against, just on the one field AT-551's own
  fix didn't touch.
- **This is a known, disclosed gap, not a fresh discovery of unknown code** — the at540 cycle-2
  checker verdict (`qa/verdicts/at540-assertion-layer.md:21-23`) says verbatim: *"1 disclosed,
  non-blocking gap: row 11 (url-probe defensive fix) has no isolating test — low severity, honestly
  disclosed by the manifest, judged as debt, not a fail."* Per protocol 4b ("a row marked UNVERIFIED
  carrying an issue id is enumerated debt") this debt should carry an issue id. **It does not** —
  `grep -n "row 11\|url-probe" qa/issues.jsonl` = 0 hits. The debt exists only in verdict prose,
  invisible to the ledger, goal-coverage, and future sweeps that read `qa/issues.jsonl` as the
  source of truth.
- Separately, **AT-551's ledger row is marked `"status":"fixed"` unqualified** — its own evidence
  text already named this exact asymmetry ("if page.url raises on a closed page that line
  propagates... breaking assert_expected's own 'raises nothing' docstring") but the fix that closed
  it only addressed the `body_text`/`absent_text` half. The `met()` url branch is unchanged from
  before the fix (confirmed via `git diff d7c2dfa..d7d8405 -- src/autotester/browser/assertions.py`
  — the `met()` `if expected.url and expected.url not in session.page.url: return False` line is
  byte-identical pre/post). Practically low blast radius (the `_url_label` helper added in the same
  commit correctly records "unmet (url unreadable)" as *evidence text*, so the grader still sees an
  honest DOM record even though `met()`'s own boolean is wrong) — but `met()`'s wrong boolean means
  polling stops after one attempt instead of retrying to `timeout_ms`, and any `dom_asserts` paired
  with a `url` expectation on the same step never gets evaluated at all when this fires.
- **proposed-id-hint:** AT-554 · **severity:** medium (silent-failure, narrower than AT-551's
  original scope, real but low-frequency trigger — a page closing/crashing mid-poll while `url` is
  declared) · **evidence:** `src/autotester/browser/assertions.py:23-44` (met, url branch
  unprotected) vs `:88-97` (`_url_label`, protected) vs `:109-117` (`body_text`, protected);
  `qa/verdicts/at540-assertion-layer.md:21-23` (disclosed gap, no issue id); `qa/issues.jsonl` line
  548 (AT-551, `status: fixed`, no scoping note that the url branch is excluded) · **suggested
  status:** open (new) — either (a) file the debt explicitly so `qa/QUEUE.md`/goal-coverage can see
  it, or (b) re-scope AT-551's `fixed` status to say "fixed for body_text/absent_text; url branch
  tracked separately."

No other silent-failure patterns found in `assertions.py` at HEAD: `selector_exists()` fails safe
(`False` on exception, matches its callers' expectations); `assert_expected()`'s own url/text
recording paths (`_url_label`, `_text_label`) both fail safe and are exception-protected;
`body_text()` fails safe. No lost-propagation (no bare re-raise), no un-timeout'd side effect (no
writes/network calls in this file — pure DOM reads).

---

## Check 8 (unnumbered in SKILL.md, grouped with shard 3) — Token ledger + code-graph blast radius

**Token ledger** — ran `python D:/ai_os/scripts/token_scan.py --project "D:/autoTesting" --days 1
--json` (read-only, no network/npm). Result (not appended to `qa/token-ledger.jsonl` — that write
belongs to consolidation):

```
opus_sub_share: 0.085 (8.5%) — under the 25% flag threshold, no finding
sub_agents: 14 today; no unit reached fix cycle 3 today (at540 topped out at cycle 2) — no fix-cycle-3 flag
classifier_outages: total 6, all "glm-5.3-flash:cloud | timed out"
loop_tools_blocked: {"ScheduleWakeup": 1}
compactions: 1 manual compaction today (pre_tokens 593342)
```

**FINDING (new, low) — one `ScheduleWakeup` call was blocked by a classifier outage today.** The
maker's self-continuation depends structurally on a real `ScheduleWakeup` call succeeding (THE
CONTINUATION RULE). `qa/.last-tick` shows continuous activity through 19:02 today so the loop did
not visibly stall this time, but a blocked continuation call during a classifier outage is exactly
the failure shape that has previously produced multi-day dead loops (AT-368, 4.85-day gap, still
open at 6 days aged — see below). Not blocking, but worth a line so a future stall has this as a
candidate cause. **proposed-id-hint:** AT-555 · **severity:** low · **evidence:** token_scan JSON
above, `loop_tools_blocked.ScheduleWakeup: 1` · **suggested status:** open (new), or fold into
AT-368 if that row is reopened as a recurring pattern.

**Code-graph blast radius** — **not-applicable.** No `graph.json` exists anywhere under the project
tree or its scratch output (`find .work -iname graph.json` and a project-tree glob both empty). Per
protocol, "Never build a graph from a sweep" — skipped, not run.

---

## Check 9 — Structural erosion (signal only, never a blocker)

- **File-churn check:** `git log --name-only` over the last 30 commits shows no file touched by 3+
  *separate* units. `assertions.py`/`test_execute.py`/`test_execute_assertions.py` each show 2 hits,
  but both are cycle-1 and cycle-2 of the *same* at540 unit (expected iteration, not churn across
  different units) — no finding.
- **Near-cap file (signal, not a violation):** `src/autotester/browser/session.py` is **299/300
  lines** — one line from the doctor's file-size cap. `assertions.py`'s own docstring already notes
  it was split out of `session.py` "for the 300-line cap (the module was at 298/300 when the
  assertion layer arrived)" — i.e. `session.py` has already needed one split and is back at the
  ceiling after +1 line since. Worth watching; the next feature that touches `session.py` will
  likely trip `autotester doctor`'s file-size rule immediately. Not filed as an issue (structural
  erosion never blocks on its own per protocol) — flagged as a watch-item for the consolidation
  report / top-3 list.
- **Duplicate implementations:** none found — `vision_ensemble()`/`_url_label()`/`_text_label()`
  helpers added this cycle are each single-purpose, no shadow of existing logic elsewhere.
- No file shows size growth across consecutive PASSes with no test added — `assertions.py` grew
  (+62/-... per the diff) with 89+31 new test lines in the same commit.

---

## Supplementary reads (within shard-3's remit, doctor/design-rule + aging)

**`uv run autotester doctor` — 1 violation (not clean):**
```
stale-generated: docs/SNAPSHOT.md — differs from regeneration; run `autotester snapshot`
1 violation(s)
```
**proposed-id-hint:** AT-556 · **severity:** low (generated-doc staleness, not a design-rule
violation of the file/function-size kind) · **evidence:** doctor output above, HEAD `93fad7b` ·
**suggested status:** open (new) — trivial fix (`autotester snapshot` regenerate + commit), likely a
side effect of the `target.md` roadmap commit (`93fad7b`) or the at540 landing not re-running
snapshot.

**Open high/critical severity issue aging** (today 2026-09-22, `status:"open"`, `severity` in
{high,critical}, sorted oldest-first, top of 15 total):
```
14d  AT-110  RunApproval tamper check defeated by recomputing id (content_id unkeyed)
13d  AT-218  GRILL: recurring vacuous-guard class, reported unchanged 3 consecutive sweeps
13d  AT-253  agent-fallback execution model is dead code (ARCHITECTURE.md doc/reality mismatch)
12d  AT-281  GRILL: D-023 moved north star after last contract amendment, T-169 no human re-check
11d  AT-335  modal crawl non-deterministic (~1/14 loses dismissed-dashboard screen)
 6d  AT-365  qa/adapter.json declares no data_class -- MC-003 gate cannot fire
 6d  AT-368  maker loop dead 4.85 days (qa/.last-tick gap)
 6d  AT-408/416/420/442/447  (bundle, all 6d)
 1d  AT-529  PATHLYNKS_USER_* creds rejected by dev server
 0d  AT-547, AT-553  (today's finds, already covered above)
```
AT-110 (14d, tamper-check defeat, high) and AT-218/AT-281 (both `GRILL:` rows, 13d/12d, already
flagged repeatedly as goal-drift by check 6 in prior sweeps) are the oldest untouched high-severity
rows — not new findings, restating for the consolidation agent's top-3 selection since aging alone
is this shard's mandate, not a fresh discovery.

---

## Terminal state

**FINDINGS: 4 new** (AT-554 met()-url-branch-debt medium, AT-555 ScheduleWakeup-blocked low,
AT-556 stale-SNAPSHOT low, plus the aging restatement of AT-110/AT-218/AT-281 as top-3 candidates —
not a new id, a priority signal). AT-541/542/543/550 independently re-confirmed open, no changes.
`autotester doctor`: 1 violation (stale generated doc, low). Code-graph: not-applicable (no graph
exists). Token ledger: no threshold breach, one low anomaly noted. Structural erosion: 1 watch-item
(session.py 299/300), no blockers.
