# Sweep verdict — 2026-09-22 (Mode B: AT-539 confirm + inbox fold + queue refresh)

Bound to `D:\autoTesting`. Window: `f9931d0..3da1f56` (the maker's AT-539 retarget commit
3da1f56, landed 2026-09-22 12:41 +05:30). Instruments re-run by this sweep, not read from any
paste. `uv run autotester doctor` -> **doctor: clean**.

## 1. AT-539 confirm — verdict: FIXED

Maker's fix verified in commit `3da1f56`: `tests/test_approve_cli.py:63/78/164` all retargeted to
`from autotester.stages.explore_consent import require_consent` (diff read directly from the
commit, three sites, same shape as `test_crawl_real_cli.py:131`).

Instruments re-run (never trusting the maker's pasted outputs):
- `uv run pytest tests/test_approve_cli.py` -> **10 passed in 0.32s, exit 0**
  (`.work/checker_at539_targeted.txt`).
- **FULL SUITE** `uv run pytest` -> **1514 passed, 5 skipped, 32 xfailed, 0 failed, exit 0**
  in 831.61s (`.work/checker_sweep_full_suite.txt`; whole-log scan for `failed|error` — no hit
  beyond the summary line's `0` failures; this was the adapter's slot-1 instrument that was
  exit 1 at AT-539 filing — it is green again).

Ledger: AT-539 flipped `open -> fixed` (append-only correction — original row left byte-intact,
`checker_note` + `fixed_date` added in-place with the full re-run evidence). Status now `fixed`;
a later re-check moves it to `verified`.

## 2. Inbox fold — 2026-09-22 TestSprite-audit entry

Every claim re-derived against this tree before filing (evidence in each row). **Filed 6 new
rows + 1 dup-resolution:**

| Issue | Sev | What | Key independent evidence |
|---|---|---|---|
| AT-540 | high | Dead assertion layer: `ExpectedState.absent_text/dom_asserts/visual_signal/network` have zero read sites; `Action.ASSERT` is `lambda session, step: None`; `_poll_for_expected` returns normally on timeout, records no failure | `rg` over `src/ tests/ scripts/`: zero consumers of the four fields; execute.py:43; session.py:230-251; pass/fail = grade.py:123 LLM-on-screenshot |
| AT-541 | medium | `CaseClass.REGRESSION_ANCHOR` unreachable | enums.py:69,87 only; absent from expand.py CLASS_DESCRIPTIONS (13 entries) + applicable_classes |
| AT-542 | medium | F-039 claims two-model ensemble; all 3 shipped erp issue rows say `models_agreeing: 1`, `model_labels: ["gemini"]` | counted 3/3 rows with the field, no other value present |
| AT-543 | medium | `oracle_human_trial` takes `duration_s: 300.0` as a call-site literal | bench.py:51 signature; sole production caller scripts/bench_trial.py:168 passes 300.0 verbatim |
| AT-544 | medium | Feature ledger 12 days stale (last row F-043/2026-09-10; crawler work through 2026-09-21 unledgered) | 43 rows, max date 2026-09-10 |
| AT-545 | low | Abandoned crawl `crawl_01M31EZVE9R1P7QDJYA6JGWYZP` stuck `status=running` (started 2026-09-21T07:48Z, no terminal status) | crawl.json read live: `running`; AT-483 orphan class |
| AT-546 | low | Contract staleness: explore.md:266 (X17c) + consent.md:35/62/68 name `require_consent` without the new `stages/explore_consent.py` seam; explore.md:280 (`:200-211`) and `:349` (`:298-300`) line anchors drifted (file now 285 lines; `_terminal_status` at :207, call site at :285) | rg over qa/contracts/; X1's own `stages/explore.py` references remain CORRECT (explore.py still owns the invented-action surface) |

**Dup resolution (not re-filed):** the ARCHITECTURE.md script-first/"~zero tokens" claim is
already carried by open high row **AT-253** (filed 2026-09-09, same three greps re-verified
today: `Script` instantiated nowhere in src/, `script_ref` only copied at case.py:60, zero
`run_with_fallback` callers in src/ — run_case_pipeline.py:95, explore.py:109 and the UI call
`execute.run_case` directly). AT-253 stays open at high with today's re-verification as
corroboration. Filing a second row would have split one defect across two ids.

**Item 2 of the inbox (ARCHITECTURE.md Execution-model correction as prerequisite)** stays a
DECISIONS-gated follow-up exactly as the inbox describes — recorded in the fold status, not
filed as a separate ledger defect (AT-253 covers the falsehood; the D-entry authorizes the prose
fix). Items 3-5 (refuse oracle-regeneration; Meticulous alternative; positioning caution) are
decision/strategy notes for the human, recorded verbatim in the inbox fold status.

## 3. Sweep checks (Mode B standard)

- **Pair-state / stale .last-tick:** `qa/.last-tick` fresh (2026-09-22 stamps in the log tail);
  x10b-form-typing manifest header reads `Status: STALLED` (cycle-3 terminal state, matching
  cfde629 verdict) with AT-539's confirm noted as "next pass" — this sweep's AT-539 confirm is
  that pass. No `ready-for-check` manifest pending a verdict. No `qa/.paused`.
- **Contract staleness for the NEW explore_consent.py seam:** filed **AT-546 low** (details
  above). The contract is NOT edited by this sweep — the amendment (re-pointing consent.md:35/
  62/68 and explore.md:266 to `stages/explore_consent.py` + fixing the drifted line anchors) is
  this checker's own next contract-touching amendment, applied with its amendment-log entry, not
  mid-verdict.
- **Enforcement liveness:** hooks present; repo history healthy (HEAD 3da1f56; doctor clean).
- **Silent-failure hunt (over the 3da1f56 diff):** the seam extraction is clean —
  `explore_consent.py` raises `ApprovalRequired` pre-browser as CN1 requires; `_poll_for_expected`'s
  swallow-on-timeout is the known AT-540 surface, not a new one. No new silent-failure rows.
- **Bypass:** 3da1f56 touches src/ + tests/ but traces to the x10b-form-typing manifest (cycle-3
  terminal) + this sweep's AT-539 confirmation; the retarget itself was pre-authorized by the
  AT-539 `expected` clause. CLEAN.

## 4. QUEUE refresh

`qa/QUEUE.md` refreshed with the 2026-09-22 top-3: **AT-540** (high, assertions-first per Umesh's
approved order; DECISIONS entry first), **AT-529** (high, HUMAN_GATE — credentials), **AT-546 +
AT-253** (checker-owned amendment + the D-entry). Deprioritised: AT-497 (joins AT-545, same
orphan-crawl fix unit can close both), AT-460, AT-488/AT-502 signals.

**Terminal state: `FINDINGS: 7`** — AT-540 high; AT-541/542/543/544 medium; AT-545/546 low.
AT-539 -> fixed on this sweep's instruments. Inbox 2026-09-22 entry folded (status appended in
place, entry left byte-intact above it). `qa/.last-sweep` stamped.