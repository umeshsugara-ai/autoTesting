# Mode B sweep report — 2026-09-22b (SHARDED; this file is the consolidation)

**Mode:** B, sharded (project > 30 source files). Three read-only shards ran (1: checks 1–3 ·
2: checks 4–6 · 3: checks 7–9 + token ledger + graph blast radius) and returned proposed
findings as text; this consolidation checker is the sweep's single writer (deduped against
`qa/issues.jsonl` and across shards, filed the rows, flipped nothing, refreshed `qa/QUEUE.md`,
stamped `qa/.last-sweep`).
**Bound root:** `D:/autoTesting` — every path read or written resolves inside it.
**Window:** `git 3833218..00e36a8` (7 commits: `eebf6bf` D-031 · `3f05fa7` snapshot · `a5e5b81`
AT-543 · `df529f2` AT-541+542 · `d7c2dfa` D-032/AT-540 · `cd3acd5` tick · `00e36a8` landed tick
+ at540 manifest) + working tree (`.goal/dashboard.html`, `.goal/goal.json`, `qa/.last-tick` —
the maker tick's own writes, untouched).
**Instrument state at this tree:** full suite `uv run pytest` → **1521 passed, 5 skipped, 32
xfailed, 0 failed, exit 0** (1113.89s, `.work/full-suite-2026-09-22-1549.txt`); `autotester
doctor` clean + `ruff check src tests scripts` clean re-verified at `00e36a8` (shard 3).
**In-flight handshake (NOT a finding):** `qa/manifests/at540-assertion-layer.md` is
ready-for-check, its Mode A checker dispatched 15:38 IST; no
`qa/verdicts/at540-assertion-layer.md` on disk at 16:10. Expected pending state; this sweep did
not touch its files and left the `execute.md` E1 / `ARCHITECTURE.md` / `grade.md` G2 amendment
decisions to that checker.

## Per-shard terminal states (as returned to the dispatcher)

| Shard | Scope | Proposed | Terminal state |
|---|---|---|---|
| 1 | checks 1–3 (bypass + pair-state, inbox fold-in, contract staleness) | bypass row; F-044 claim row; x10b stall row; track-only note on execute.md E1 | FINDINGS: 3 proposed |
| 2 | checks 4–6 (enforcement liveness, data boundary, goal coverage, drift/re-grill) | F-044 claim (dup of shard 1); grade.md G2 fold proposal; row-state confirmations | FINDINGS: 1 proposed (deduped); checks 4/5/6 CLEAN — loop.md matches adapter + state table, goal coverage complete, no re-grill due, 3 GRILL rows carried |
| 3 | checks 7–9 + token ledger + graph | opus share; AT-542 ensemble shrink; assertions suppress; inbox hygiene | FINDINGS: 4 proposed; checks 8/9 CLEAN — doctor/ruff clean at HEAD, AT-488/AT-502 unchanged in window (no refile), no graph.json → NOT-APPLICABLE |

## Findings filed (final ids; one row per distinct defect)

| ID | Sev | Finding (one line) |
|---|---|---|
| AT-547 | high | Bypass: AT-541+AT-542 fixes landed in `df529f2` and AT-543 in `a5e5b81` with no manifest and no verdict; ledger rows stay open (a flip needs a verdict); remediation = retro-manifests + Mode A checks. |
| AT-548 | medium | `df529f2`'s message claims "+ F-044 ledger correction appended"; `docs/FEATURES.jsonl` has no F-044 row and the commit never touches the file (last row F-043/2026-09-10). |
| AT-549 | medium | `x10b-form-typing` terminal-STALLED (manifest :230, FAIL at Cycle checked: 3 of max 3) with no `qa/debug/x10b-form-typing*` report — stall without diagnosis; its sole FAIL cause (AT-539) is since fixed, instrument green, disposition missing. |
| AT-550 | medium | Ensemble wiring silently shrinks: `ui/routes_sources.py:237-241` drops uncredentialed providers with no recorded signal; `schema/project.py:61-70` substitutes `["gemini"]` for an empty config (extends open AT-542). |
| AT-551 | medium | `browser/assertions.py` `met()` swallows probe errors into True (:25-39) and `body_text()` returns "" on a broken page (:84-87) — an absent_text-only expectation on a crashed page records `assert absent_text: met` and the run completes clean; visible_text/dom_asserts fail safe. |
| AT-552 | low | Inbox residual: umesh's 2026-09-06 home-page-dashboard ask was never filed; the two sibling 2026-09-07 asks were delivered and are now Status-annotated. |

## Dedupes and dispositions (the consolidation's actual decisions)

1. **F-044 claim (shard 1 + shard 2, colliding id proposals) → ONE row, AT-548.** Verified
   myself: `git show df529f2 --stat` names 6 files, none `docs/FEATURES.jsonl`;
   `git show df529f2 -- docs/FEATURES.jsonl` is empty; grep F-044 = 0 hits.
2. **opus_sub_share 0.289 (shard 3) → NOT a new row.** The metric already has an open row
   (AT-486, "audit the day's Opus dispatches"); filing a second row for a recurring reading of
   the same metric would duplicate. Consolidation re-read at 16:10:25 gives **0.153 — UNDER the
   0.25 line** (the day-window shifted: ~3.5M Opus of 22.9M sub tokens, 6 sub agents, 1 of them
   the in-flight at540 checker; a final scan at 16:27:47 reads 0.109 — further under). Both
   readings recorded on AT-486's row; the scan line appended to `qa/token-ledger.jsonl`
   (`scanned_at 2026-09-22T16:27:47`, `opus_sub_share` 0.109, 6 sub agents). The row stays
   open: the dispatch audit it asks for is still not performed. (The scan also captured 3
   harness-side classifier outages — harness, not a project finding.)
3. **grade.md G2 + execute.md E1 (shards 2 and 1) → folded into the in-flight at540 checker's
   contract touch; no rows filed.** Verified: `grade.md` G2 ("Only `Outcome.COMPLETED` results
   are sent to the judge") contradicts shipped D-032 (grade.py short-circuits only
   BLOCKED_HITL/ERRORED; ASSERTION_FAILED reaches the judge by design), and `execute.md` E1
   still carries the pre-D-032 three-outcome/no-compare language. The at540 manifest :14-15
   declares the E1 amendment as its contract touch; G2 is the same D-032 wave. If that
   checker's verdict lands without them, the next sweep files the residuals.
4. **AT-542 ensemble silent-shrink (shard 3) → NEW row AT-550, extending open AT-542** (distinct
   defect: runtime silent degradation vs AT-542's ledger-overclaim; one row per distinct
   defect). Shard 3's `project.py:363` anchor was wrong — the code sits at :61-70
   (`return seen or ["gemini"]` at :70); corrected in the row.
5. **assertions met()-suppress (shard 3) → ruled a DEFECT, not contract-asked → AT-551
   (medium).** Judged against D-032's Result section: the contract asks for "raises nothing"
   AND honest `assert <field>: met|unmet` facts. No-raise is satisfiable while failing SAFE
   (return False on probe error; name the error in the recorded detail). Recording `met` from a
   swallowed exception is the false-PASS defence D-032 exists to provide, inverted. Blast
   radius is narrow (only absent_text/url can mint false-met on a dead page — visible_text and
   dom_asserts fail safe; the crash still reaches the LLM judge via the screenshot), hence
   medium, not high. The module's own docstring ("RECORD facts, never assign PASS/FAIL") argues
   the same way.
6. **Inbox hygiene (shard 3) → fold bookkeeping + one residual row.** `qa/feedback-inbox.md`
   lines 344–346 (the three inline umesh entries) now carry **Status:** lines: the 2026-09-07
   back-nav and live-preview asks are annotated DELIVERED (unit ui-back-nav-and-live-clarity,
   verdict on disk; the empty-project dead-end half is AT-057, verified); the 2026-09-06
   home-dashboard ask is annotated FILED as **AT-552** (verified unfiled: grep for the ask's
   signature phrases over the ledger returns nothing).
7. **Row-state, no flips (protocol: a flip needs a verdict).** Shard 2's code-half
   confirmations re-verified at HEAD: `expand.py:33` REGRESSION_ANCHOR declared + `:76`
   appended (AT-541); `project.py:61-70` vision_ensemble + `routes_sources.py:237-244` wiring
   (AT-542); `bench.py:52` `duration_s: float | None = None` + `bench_trial.py:147` measured
   duration (AT-543). Evidence notes appended to all three rows; **they stay open** and are
   carried by the AT-547 bypass row. **AT-253 NOT flipped** (D-031 landed the ARCHITECTURE
   prose; the agent-fallback wiring question stays gated). **AT-415 re-confirmed** (gates
   unchanged). Fixed-vs-verified counts at write time: open 159 / fixed 163 / verified 206 —
   fixed is NOT ahead of verified; no claims-outrunning-checks finding.
8. **Checks clean (nothing filed):** enforcement liveness (loop.md Stop/Human-gate lines match
   the adapter + state table); goal coverage (north-star decomposition complete); no re-grill
   due (north star unchanged, no off-disk gate answers; 3 GRILL rows carried — AT-218, AT-281,
   AT-402); `x10b` is the only STALLED manifest without its `qa/debug/` cycle-3 report
   (at015/at345/at379/at438 have theirs); maker liveness: `qa/.last-tick` fresh (15:22);
   data-boundary gate still fires (no `data_class` in `qa/adapter.json`) — standing open high
   row AT-365, not re-filed; code graph: no `graph.json` in scratch → update/query
   NOT-APPLICABLE (never build a graph from a sweep).

## Reopen-power

Not exercised: no unit's claim was found unbacked this sweep. The bypass defect is a process gap
(file AT-547); the unverified fixes were never claimed verified, so nothing needed reopening.

## Terminal state

**FINDINGS: 6** — AT-547 high; AT-548/549/550/551 medium; AT-552 low; 0 flips; AT-486 extended
with both token readings; inbox entries annotated; token-ledger line appended.

## Top-3 recommended next units

1. **at540-assertion-layer Mode A check** — in flight; not to be raced. Its contract touch
   (execute.md E1 per D-032) should also absorb the grade.md G2 line and, if convenient, the
   AT-546 require_consent re-point.
2. **AT-547 (high) bypass remediation** — retro-manifests + Mode A checks for
   AT-541/AT-542/AT-543.
3. **The buildable tail:** AT-529 (high, HUMAN_GATE) · AT-546 checker re-point · AT-544 via the
   F-044 remediation (AT-548) · AT-545+AT-497 heartbeat · T-123 medium batch · AT-550/AT-551
   follow-ons.

— Consolidation checker, /checker Mode B (sharded), 2026-09-22, at `00e36a8`.