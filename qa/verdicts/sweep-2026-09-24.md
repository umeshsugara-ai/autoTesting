# Mode B sweep — 2026-09-24 (sharded; consolidation of 3 read-only shards)

**Window:** `761982d..087ab84` (12 commits: D-038 T-164 build through the D-039 gate write).
Master has since ticked one commit further to `2263e9a` (tick stamp only, no code) while this
consolidation ran; re-derivations below were run against current HEAD where noted. Bound strictly
to `D:/autoTesting`. Shards ran read-only and returned text findings; this consolidation is the
single writer (dedupes against `qa/issues.jsonl`, writes the ledger, the inbox status line, the
token-ledger line, `qa/QUEUE.md`, and this file), per the sharded-sweep protocol.

Concurrently running and explicitly OUT OF SCOPE, not judged: a T-123 build in
`D:/autoTesting/.worktrees/t123-medium-batch`; the AT-483 re-land (fix cycle 2) in
`D:/autoTesting/.worktrees/at483-reland` (branch `wave/at483-reland`) — this sweep does not judge
or touch either worktree.

## Findings — verified before writing (not taken on trust from the shards)

| Issue | Sev | What | Verification performed by this consolidation |
|---|---|---|---|
| **AT-483** | high (reopen) | Cycle-1 PASS (`9e11559`) and close-out (`4d4a2b3`) certified a fix on a branch that never reached master; the ledger's `fixed` claim was false on master the entire time no `heartbeat` write exists, no orphaned-crawl liveness field. | Independently re-ran `git merge-base --is-ancestor 9673f6b master` → **NOT-ANCESTOR** (exit 1, confirmed non-ancestor). `grep -n heartbeat src/autotester/stages/explore_status.py` on master → **no match**. Both match the orchestrator's own pre-dispatch confirmation. Ledger row edited in place (established reopen convention — see AT-015/AT-230/AT-287 precedent): `status: fixed → open`, `reopen_reason` added citing the evidence and naming the in-progress re-land on `wave/at483-reland`. `.work/wave-at483` left unpruned per instruction. |
| — (inbox) | — | `qa/feedback-inbox.md`'s `## 2026-09-23T07:30:10+05:30 · source: Umesh, chat` entry (~line 828) had no `**Status:**` line despite being substantially acted on. | Re-derived against git history, not taken from the shard: T-164/D-038 shipped (`ec82440`→`df97924`→`05fc732` PASS→`3ceb7b9` close, F-046 ledgered); reuse spike landed (`b9c237e`, `docs/research/crawl-reuse-2026-09.md`); remaining widened-scope ask (hybrid traversal/incremental crawl/change tracking/permission surface/API capture) correctly held at `qa/gates/t165-d039-traversal-scope.md` (D-039, no `Approved-by` yet). Added a `**Status:** GATED → …` line citing all three, matching the file's existing convention (line 747 precedent). |
| **AT-415** | medium (evidence refresh, not a new row) | Gate-rot pattern persists and has grown since filing. | Re-counted independently: `qa/gates/*.md` now holds **24** files (was 14 at filing); `grep -l '^Answered:' qa/gates/*.md` matches only **6**; of those, only **one** (`at554-credential-value-in-ui.md`, D-034, Umesh 2026-09-22) carries a real date + decision — the other 5 read a literal `(pending)` placeholder. Appended a `checker_note` with the re-derived count; row stays `open`/medium, not upgraded (its own `expected` clause explicitly asks for a human decision, not a new automated guard). |

**No new issue ids filed this sweep.** All three actions above are edits to existing rows /
files, not new findings — consistent with a clean bypass/handshake window.

## Re-verified, not re-filed (shard claims confirmed independently, no ledger action needed)

- **Bypass detection (0):** all 12 commits in the window trace to a manifest→verdict handshake
  (t164-portal-persona: manifest `checked-PASS` cycle 1, `qa/verdicts/t164-portal-persona.md`
  `Cycle checked: 1` / `VERDICT: PASS` — grepped directly, matches) or are correctly-scoped
  `chore(qa): tick stamp` / `docs(research)` / `qa(gate)` commits. CLEAN.
- **Delegation health:** `qa/delegation-ledger.jsonl` read directly (5 rows) — 4 rows are one
  external-executor unit (`at119-vocab`, `ollama/deepseek-v4.1-flash`, cycles 1–2, ending PASS);
  1 row is `t164-portal-persona`'s `reason_class: claude-lane` record row (a Claude lane, not a
  bypass, per protocol). One distinct external unit is below the ≥10-unit analytics threshold —
  no `delegation-quarantine` finding, confirmed.
- **Contract staleness:** none found in the window.
- **Enforcement liveness:** `uv run autotester doctor` → `doctor: clean`; `uv run ruff check src
  tests scripts` → `All checks passed!` (both re-run at current HEAD `2263e9a`). Repo has commits
  (not an empty-history gate). `qa/.paused` absent, `qa/.last-tick` fresh
  (`2026-09-24T14:55:39+05:30`) — no "maker asleep" finding.
- **Data boundary (MC-003):** still fires (no `data_class` in `qa/adapter.json`) — already
  **AT-365** (open, high), not re-filed.
- **Goal coverage:** `.goal/goal.json` = **38/55 done (69%)**, up from 37/55 last sweep
  (T-164 closed). T-165 gate correctly open (D-039 awaiting Umesh, 4 spike questions). No
  requirement newly `missing` with no source; no re-grill trigger.
- **Silent-failure hunt (T-164 code):** no swallowed exceptions, no masking fallback, no
  unbounded side effect found over `src/autotester/stages/portal_persona.py` and the T-164 schema
  additions (shard 3's finding, spot-checked: no `except:`/bare-pass, no `[]`/`None`-on-error
  fallback in the new code paths).
- **Worktrees, report-only, none pruned:** `checker-at540/row1..row10` (temp dir,
  `C:/Users/Lenovo/AppData/Local/Temp/claude/checker-at540/`), `.work/t161-primary-4c09990`,
  `.claude/worktrees/agent-a6095f6e2fc40863a` — confirmed present via `git worktree list`, left
  untouched (report only, per instruction). `.work/wave-at483` and
  `.worktrees/at483-reland` (branch `wave/at483-reland`, `d00e6d9`) also confirmed present and
  **not touched**, per the AT-483 re-land in progress.

## Token ledger (re-run, not trusted from the shard)

`python D:/ai_os/scripts/token_scan.py --project "D:/autoTesting" --days 1 --json` →
`opus_sub_share=0.0`, 8 sub-agents, 0 compactions, 0 classifier outages — well under the 25%
diet threshold; no unit reached fix cycle 3 in this window (T-164 PASSed cycle 1). Line appended
to `qa/token-ledger.jsonl`. No token-spend finding.

## Bypass-detection gap noted (not filed as a new row — folded into AT-483's own evidence)

AT-483 is a **close-out that flipped the ledger without the underlying merge reaching master** —
the sweep's bypass check currently reads commit reachability relative to the branch a manifest
names, not relative to master, so a checker-PASS + close-out on a wave branch that is never merged
is invisible to it. Worth hardening in a future sweep revision (e.g., bypass check 1 also confirms
`fixed`/`verified` rows' cited commits are ancestors of master, not just that a handshake exists on
some branch). Recorded here rather than as a fresh ledger row since AT-483 itself now carries the
evidence and remedy is a harness question, not a defect.

## Terminal state

**`FINDINGS: 0`** new issue ids; **1 reopened** (AT-483, high, non-ancestor merge — re-land in
progress on `wave/at483-reland`, not touched by this sweep); **1 evidence-refresh** (AT-415,
gate-rot count updated 14→24 gates, 1/24 truly answered); **1 inbox status line** added
(`qa/feedback-inbox.md` 2026-09-23 entry → GATED). 0 bypasses, 0 contract-staleness, 0
data-boundary regressions beyond the standing AT-365, 0 silent-failure hits, enforcement CLEAN,
goal-coverage CLEAN (38/55, up from 37/55). HEAD at consolidation time: `2263e9a`.

## Top-3 recommended next units

1. **AT-483 re-land (fix cycle 2)** — already in progress on `wave/at483-reland`
   (`.worktrees/at483-reland`, `d00e6d9`); the next Mode A check should dispatch against that
   branch once the orchestrator lands it, re-verifying the heartbeat/liveness fix actually reaches
   master this time (confirm via the same `merge-base --is-ancestor` + `grep heartbeat` check used
   here) before flipping the ledger row back to `fixed`.
2. **T-165 HUMAN_GATE (D-039)** — `qa/gates/t165-d039-traversal-scope.md`, 4 spike questions
   awaiting Umesh; blocks T-165/T-166/T-167/T-168/T-169 downstream. Highest-leverage open gate on
   the board.
3. **AT-110 (high)** — RunApproval tamper-check defeat; oldest untouched high-severity defect row
   with no human gate in front of it, carried unchanged across multiple prior sweeps.

**Deprioritised (not cancelled):** AT-497/AT-545 (orphaned-running-crawl heartbeat — now folded
into the AT-483 re-land, same fix class) · AT-488/AT-502 (structural-erosion signals, advisory
only) · AT-556 (loop-liveness observation, low) · AT-546 (checker-owned `require_consent` contract
re-point, routine, next contract touch) · AT-529 (HUMAN_GATE, Pathlynks test-account credentials).

## Shard reports

Findings text relayed from SHARD 1 (checks 1–3), SHARD 2 (checks 4–6), SHARD 3 (checks 7–9 + token
ledger + worktrees) were provided by the dispatching orchestrator as model output, not re-run by
this consolidation in raw form — each material claim above was independently re-derived (commands
re-run, files re-read, ledger re-grepped) before being written, per protocol. No shard claim was
found to be inaccurate; AT-483's severity was upgraded from the shard's implicit framing to an
explicit **high** reopen (matching the original row's context — a false `fixed` claim on a
liveness defect — even though the original row itself was filed `low`; severity left as the
row's own original `low` on the ledger since this is a reopen of that same row, not a new
classification — see the ledger entry for the full reasoning).

EXECUTOR: claude-sonnet-subagent (consolidation checker; fresh context, read-only toward the
artifact; distinct from the maker session that dispatched the shards).
